/**
 * Конфиг api
 * @example
 * exampleKey: {
 *    baseUrl: 'https://outside-url.com', // можно не добавлять тогда будет дефолтный url
 *    GET: '/data/item',
 *    POST: '/data/item',
 *    PUT: '/data/:itemId',
 *    DELETE: '/data/:itemId',
 *  },
 *
 */
const configApi = {
  example: {
    // Комментарий для чего используется
    GET: "/example",
  },
};

export default configApi;
